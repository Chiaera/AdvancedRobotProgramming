gcc -Wall -Wextra main.c -o main  -lncurses
 ./main

make
    ./build/blackboard
    make clean

rm -rf build blackboard
    gcc -Wall -Wextra -g -Iinclude \
    src/blackboard.c src/map.c src/input.c src/drone.c \
    -lncurses -o blackboard
        ./blackboard

assignment1/
    │
    ├── src/
    │   ├── blackboard.c
    │   ├── map.c
    │   ├── input.c
    │   ├── drone.c
    │   ├── obstacles.c     (più avanti)
    │   ├── targets.c       (più avanti)
    │   ├── config.c        (più avanti)
    │   └── world.c         (fisica - più avanti)
    │
    ├── include/
    │   ├── map.h
    │   ├── input.h
    │   ├── drone.h
    │   ├── obstacles.h
    │   ├── targets.h
    │   ├── config.h
    │   └── world.h
    │
    ├── bin/
    │   ├── parameters.cnf
    │   ├── log.txt
    │   └── runtime dumps (qualunque file generi)
    │
    ├── build/
    │   ├── obj/     (file .o)
    │   └── bin/     (eseguibili finali)
    │
    └── Makefile



CONFIG - MAP - blackboard
    map.c       → modulo grafico (GUI) -> si occupa solo della finestra
    map.h       → interfaccia pubblica del modulo grafico

    config.c    → parser del file config -> legge parametri
    config.h    → interfaccia del parser (struttura Parametri + load_config)

    blackboard.c    → gestisce tutti i processi + blackboard

FASE 1
    /project
    │
    ├── blackboard.c
    ├── map.c
    ├── map.h
    ├── input.c
    ├── input.h
    ├── drone.c
    ├── drone.h
    └── Makefile  

GITHUB
    git add . (. or nameFile)
    git commit -m "message of commit"
    git push -u origin main
    
