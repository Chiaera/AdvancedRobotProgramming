assignment1/
    │
    ├── src/
    │   ├── blackboard.c
    │   ├── map.c
    │   ├── input.c
    │   ├── drone.c
    │   ├── obstacles.c     
    │   ├── targets.c    
    │   └── world.c        
    │
    ├── include/
    │   ├── map.h
    │   ├── input.h
    │   ├── drone.h
    │   ├── obstacles.h
    │   ├── targets.h
    │   └── world.h
    │
    ├── bin/
    │   ├── parameters.cnf
    │   ├── log.txt --> NON ANCORA
    │   └── runtime dumps --> NON ANCORA
    │
    ├── build/
    │   ├── obj/     (file .o)
    │   └── bin/     (eseguibili finali)
    │
    └── Makefile

CONFIG - MAP - blackboard
    map.c       → modulo grafico (GUI) -> si occupa solo della finestra
    map.h       → interfaccia pubblica del modulo grafico

    config.c    → parser del file config -> legge parametri
    config.h    → interfaccia del parser (struttura Parametri + load_config)

    blackboard.c    → gestisce tutti i processi + blackboard

GITHUB
    git add . (. or nameFile)
    git commit -m "message of commit"
    git push -u origin main
    
COMPILE
    make clear (r, .rf build)
    make
    make run

DA FARE
    - forze repulsive
    - verificare universalità path
    - sistemare collisione drone-target 
    - servirà  mai semaforo (?)